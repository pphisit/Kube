apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-ingress
  namespace: test
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - match: Host(`test2.com`)
    kind: Rule
    services:
    - name: nginx-service
      port: 80